const config = {
    content: ["./src/**/*.{html,js,svelte,ts}"],

    theme: {
        extend: {
            colors: {
                "svelte-orange": "#ff3e00",
            },
        },
    },

    plugins: [],
}

module.exports = config
